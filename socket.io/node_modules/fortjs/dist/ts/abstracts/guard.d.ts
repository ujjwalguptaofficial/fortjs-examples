import { IHttpResult } from "../interfaces";
import { Logger } from "../models";
import { IGuardTestData } from "../helpers";
import { Component } from "./component";
export declare abstract class Guard extends Component {
    get body(): {
        [key: string]: any;
    };
    get param(): {
        [key: string]: string;
    };
    get file(): import("../models").FileManager;
    get workerName(): string;
    abstract check(...args: any[]): Promise<IHttpResult | void>;
    get logger(): Logger;
    get option(): import("./component_option").ComponentOption;
    constructor(...args: any[]);
    initialize(data?: IGuardTestData): import("./controller").Controller;
}
