import { IHttpResult } from "../interfaces";
import { IWallTestData } from "../helpers";
import { Component } from "./component";
export declare abstract class Wall extends Component {
    abstract onIncoming(...args: any[]): Promise<IHttpResult | void>;
    onOutgoing(finalResult: IHttpResult, ...args: any[]): void;
    constructor(...args: any[]);
    initialize(data?: IWallTestData): import("./controller").Controller;
}
