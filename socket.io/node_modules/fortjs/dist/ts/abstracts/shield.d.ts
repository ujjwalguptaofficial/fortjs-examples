import { IHttpResult } from "../interfaces";
import { Logger } from "../models";
import { IShieldTestData } from "../helpers";
import { Component } from "./component";
export declare abstract class Shield extends Component {
    get workerName(): string;
    get logger(): Logger;
    get option(): import("./component_option").ComponentOption;
    abstract protect(...args: any[]): Promise<IHttpResult | void>;
    constructor(...args: any[]);
    initialize(data?: IShieldTestData): IShieldTestData;
}
