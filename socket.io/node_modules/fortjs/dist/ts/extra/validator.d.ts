import { ValidatorOptions } from "class-validator";
import { IDtoValidator } from "../interfaces";
import { ErrorResultMapper } from "../types";
export declare class DtoValidator implements IDtoValidator {
    validatorOption: ValidatorOptions;
    getErrorResult: ErrorResultMapper;
    constructor(validatorOption?: ValidatorOptions, errorResultMapper?: ErrorResultMapper);
    validate(dtoInstance: any): Promise<string>;
}
