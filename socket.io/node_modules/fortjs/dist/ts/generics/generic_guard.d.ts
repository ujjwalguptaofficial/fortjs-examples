import { Guard } from "../abstracts";
import { IHttpResult } from "../interfaces";
export declare class GenericGuard extends Guard {
    constructor(...args: any[]);
    check(...args: any[]): Promise<IHttpResult>;
}
