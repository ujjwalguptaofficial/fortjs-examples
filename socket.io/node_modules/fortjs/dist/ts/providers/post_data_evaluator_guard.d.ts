import { Guard } from "../abstracts";
export declare class PostDataEvaluatorGuard extends Guard {
    check(): Promise<import("../interfaces").IHttpResult>;
    handlePostData(): Promise<any[]>;
    private getPostRawData_;
    private parseMultiPartData_;
    parsePostData(): Promise<any[]>;
}
