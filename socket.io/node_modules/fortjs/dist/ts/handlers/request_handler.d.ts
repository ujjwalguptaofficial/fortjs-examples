/// <reference types="node" />
import * as http from "http";
import { ControllerResultHandler } from "./controller_result_handler";
export declare class RequestHandler extends ControllerResultHandler {
    private routeMatchInfo_;
    private wallInstances;
    private registerEvents_;
    private executeWallIncoming_;
    runController_(): Promise<() => Promise<unknown>>;
    private executeShieldsProtection_;
    private executeGuardsCheck_;
    private setPreHeader_;
    private onRouteMatched_;
    private runWallOutgoing_;
    private execute_;
    handle(request: http.IncomingMessage, response: http.ServerResponse): void;
    setControllerProps_(): any;
}
